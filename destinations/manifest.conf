# Destination manifest
# One destination name per line.
# Adding a destination requires:
#   1. Add its name to this file
#   2. Create destinations/<name>/module.sh with lifecycle hooks
#   3. Add config defaults to destinations/<name>/destination.conf
local
onedrive
