# Local destination configuration
# Syncs the Restic repository to a local path (e.g. Umbrel SSD).
# Path must be writable by the user running the backup.
LOCAL_DESTINATION_PATH="/mnt/umbrel/backups/restic"
