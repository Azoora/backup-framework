# Service manifest
# One service name per line.
# Adding a service requires:
#   1. Add its name to this file
#   2. Create services/<name>/module.sh  with lifecycle hooks
#   3. Create config/services/<name>.conf with user-settable overrides
vaultwarden
