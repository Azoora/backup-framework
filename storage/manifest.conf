# Storage manifest
# One storage provider name per line.
# Adding a storage provider requires:
#   1. Add its name to this file
#   2. Create storage/<name>/module.sh with lifecycle hooks
#   3. Create config/services/<name>.conf with user-settable overrides
onedrive
umbrel
